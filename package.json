{
  "name": "issue-tracker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "cd Backend && ..\\env\\Scripts\\python.exe -m uvicorn main:app --reload",
    "dev:frontend": "cd FrontEnd && npm run dev"
  },
  "devDependencies": {
    "concurrently": "9.1.2"
  }
}
